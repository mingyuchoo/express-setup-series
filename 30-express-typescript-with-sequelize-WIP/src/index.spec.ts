import { expect, should } from 'chai';
should();

describe('example', () => {
  it('expect example', () => {
    expect(true).to.be.true;
  });

  it('should example', () => {
    true.should.be.true;
  });
});
